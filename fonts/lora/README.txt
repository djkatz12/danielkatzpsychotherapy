Local Lora font files are generated by running:

    make fonts

This package intentionally does not ship the mismatching Fontsource Lora files.
The script fetches the exact CSS/files returned by the Google Fonts request used
by the original site, then rewrites the site to use those files locally.
